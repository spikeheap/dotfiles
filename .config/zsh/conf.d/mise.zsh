# mise — runtime version manager (replaces asdf). Reads .tool-versions and,
# for the tools enabled in ~/.config/mise/config.toml, idiomatic files such as
# .ruby-version / .nvmrc. Activation sets up shims, PATH and the chpwd hook.
_cache_eval mise mise activate zsh
